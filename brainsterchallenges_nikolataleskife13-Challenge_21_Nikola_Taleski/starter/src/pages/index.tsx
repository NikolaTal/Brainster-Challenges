import type { GetStaticProps, NextPage } from "next";
import Head from "next/head";
import Banner from "../components/Banner";
import CategoryPicker from "../components/CategoryPicker";
import FeaturedBlogs from "../components/FeaturedBlogs";
import FeaturedProducts from "../components/FeaturedProducts";
import { IBanner, IBlogs, IProduct } from "../types";

interface Props {
  dataBanner: IBanner;
  dataFeaturedProducts: IProduct[];
  dataFeaturedBlogs: IBlogs[];
}

const Home: NextPage<Props> = ({
  dataBanner,
  dataFeaturedProducts,
  dataFeaturedBlogs,
}) => {
  return (
    <>
      <Head>
        <title>Store</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <Banner {...dataBanner} />

      <CategoryPicker />

      <FeaturedProducts products={dataFeaturedProducts} />

      <FeaturedBlogs blogs={dataFeaturedBlogs} />
    </>
  );
};

export default Home;

export const getStaticProps: GetStaticProps = async () => {
  const resBanner = await fetch("http://localhost:5001/banner_content");
  const dataBanner: IBanner = await resBanner.json();

  const resFeaturedProducts = await fetch(
    "http://localhost:5001/products/?_limit=4"
  );
  const dataFeaturedProducts: IProduct[] = await resFeaturedProducts.json();

  const resFeaturedBlogs = await fetch("http://localhost:5001/blogs/?_limit=3");
  const dataFeaturedBlogs: IBlogs[] = await resFeaturedBlogs.json();

  return {
    props: {
      dataBanner,
      dataFeaturedProducts,
      dataFeaturedBlogs,
    },
  };
};
